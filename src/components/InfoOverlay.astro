---
import { SITE } from '../data/site';
import { services } from '../data/services';
import RequestForm from './RequestForm.astro';
---
<!-- Top chrome over the gallery (visible when the menu is closed) -->
<div class="topbar">
  <a class="wordmark" href="/" aria-label={`${SITE.name} — home`}>
    <img src="/logo.webp" alt={SITE.name} width="116" height="54" />
  </a>
  <button type="button" class="menu-trigger" data-info-open aria-haspopup="dialog">Menu</button>
</div>

<!-- Persistent CTA on the gallery — opens the menu's brief form -->
<button type="button" class="cta-fab" data-open-contact>Create new product</button>

<!-- The menu doubles as the homepage: it's open by default on first load -->
<div class="info" id="info" role="dialog" aria-modal="true" aria-label="Menu" aria-hidden="true" hidden>
  <button type="button" class="info__close" data-info-close aria-label="Close menu">
    <span aria-hidden="true">×</span>
  </button>

  <div class="info__inner">
    <img class="info__logo" src="/logo.webp" alt={SITE.name} width="160" height="74" />

    <div class="info__about">
      <p>PPC is a creative merchandise production studio.</p>
      <p>
        We help brands turn ideas into high-quality products by selecting the
        most suitable garments and items for each project, then bringing them to
        life through premium printing and embroidery.
      </p>
      <p>When timing is critical, we can deliver complete projects in as little as 3 business days.</p>
    </div>

    <nav class="info__buttons" aria-label="Sections">
      <button type="button" class="info__btn info__btn--primary" data-info-close>Gallery</button>
      <a class="info__btn" href="/catalog">Catalog</a>
      <a class="info__btn" href="/studio">Studio</a>
    </nav>

    <section class="info__brief" id="brief">
      <h2 class="info__h">Start a project</h2>
      <RequestForm />
    </section>

    <nav class="info__links" aria-label="Techniques">
      {services.map((s) => <a href={s.path}>{s.name}</a>)}
    </nav>

    <ul class="info__contact">
      <li><span>Studio</span>{SITE.address.full} · {SITE.address.district}</li>
      <li><span>WhatsApp</span><a href={SITE.whatsapp} target="_blank" rel="noopener noreferrer">{SITE.phone}</a></li>
      <li><span>Telegram</span><a href={SITE.telegramUrl} target="_blank" rel="noopener noreferrer">{SITE.telegram}</a></li>
      <li><span>Email</span><a href={`mailto:${SITE.email}`}>{SITE.email}</a></li>
      <li><span>Instagram</span><a href={SITE.instagramUrl} target="_blank" rel="noopener noreferrer">{SITE.instagram}</a></li>
    </ul>

    <p class="info__legal">{SITE.legalName} · NIPC {SITE.nipc} · VAT {SITE.vatId}</p>
  </div>
</div>

<style>
  .topbar {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 40;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: clamp(1rem, 3vw, 2rem) clamp(1rem, 5vw, 5rem);
    pointer-events: none;
  }
  .topbar > * { pointer-events: auto; }
  .wordmark { line-height: 0; }
  .wordmark img { height: clamp(2.75rem, 5.5vw, 4.5rem); width: auto; }

  .menu-trigger {
    border: 0; background: transparent;
    font-size: 0.875rem; font-weight: 700;
    text-transform: uppercase; letter-spacing: 0.12em;
    color: var(--color-accent); cursor: pointer;
    padding: 0.25rem 0; transition: opacity 0.2s ease;
  }
  .menu-trigger:hover { opacity: 0.65; }

  .cta-fab {
    position: fixed;
    left: 50%; bottom: clamp(1.25rem, 4vw, 2.5rem);
    transform: translateX(-50%);
    z-index: 40;
    font-family: var(--font-sans); font-weight: 700;
    font-size: 0.9375rem; letter-spacing: 0.02em; white-space: nowrap;
    color: var(--color-accent); background: #fff;
    border: 2.5px solid var(--color-accent); border-radius: 50%;
    padding: 0.9rem 2.3rem; cursor: pointer;
    transition: background 0.2s ease, color 0.2s ease;
  }
  .cta-fab:hover { background: var(--color-accent); color: #fff; }

  .info {
    position: fixed; inset: 0; z-index: 60;
    background: #fff; overflow-y: auto;
    opacity: 0; transition: opacity 0.4s ease;
  }
  .info.is-open { opacity: 1; }
  .info[hidden] { display: none; }

  .info__close {
    position: fixed;
    top: clamp(1rem, 3vw, 2rem); right: clamp(1rem, 3vw, 2rem);
    width: 2.75rem; height: 2.75rem;
    display: flex; align-items: center; justify-content: center;
    border: 0; background: transparent;
    font-size: 2rem; line-height: 1;
    color: var(--color-text); cursor: pointer; z-index: 2;
  }
  .info__close:hover { color: var(--color-accent); }

  .info__inner {
    max-width: 720px;
    margin: 0 auto;
    padding: clamp(3rem, 8vh, 5rem) clamp(1.25rem, 6vw, 3rem) clamp(3rem, 8vh, 5rem);
    display: flex;
    flex-direction: column;
    gap: clamp(2rem, 5vh, 3rem);
  }
  .info__logo {
    align-self: center;        /* don't stretch in the column flex */
    height: clamp(2.5rem, 6vw, 3.5rem);
    width: auto;
    max-width: 100%;
  }

  .info__about {
    text-align: center;
    max-width: 44ch;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
  }
  .info__about p {
    margin: 0;
    font-size: clamp(1.0625rem, 2.1vw, 1.3125rem);
    line-height: 1.5;
    color: var(--color-text);
  }

  /* Three primary destinations — oval buttons echoing the PPC logo */
  .info__buttons {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.85rem;
  }
  .info__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 15rem;
    padding: 0.9rem 2.25rem;
    border: 2px solid var(--color-accent);
    border-radius: 50%;
    background: #fff;
    color: var(--color-accent);
    font-weight: 700;
    font-size: 1.0625rem;
    letter-spacing: 0.01em;
    text-decoration: none;
    cursor: pointer;
    transition: background 0.2s ease, color 0.2s ease, border-color 0.2s ease;
  }
  .info__btn:hover { background: var(--color-accent); color: #fff; }
  .info__btn--primary { background: var(--color-accent); color: #fff; }
  .info__btn--primary:hover { background: var(--color-text); border-color: var(--color-text); }

  .info__brief { border-top: 1px solid var(--color-rule); padding-top: clamp(1.75rem, 4vh, 2.5rem); }
  .info__h {
    margin: 0 0 1.25rem;
    font-size: 0.8125rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .info__links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 1.25rem;
  }
  .info__links a { font-size: 0.9375rem; color: var(--color-text); }

  .info__contact { list-style: none; padding: 0; margin: 0; font-size: 0.9375rem; }
  .info__contact li { margin-bottom: 0.4rem; color: var(--color-text); }
  .info__contact span { display: inline-block; width: 6rem; color: var(--color-mute); }

  .info__legal {
    margin: 0;
    font-size: 0.75rem;
    color: var(--color-faint);
  }
</style>

<script>
  function initInfo() {
    const info = document.getElementById('info');
    if (!info) return;
    let lastFocus: HTMLElement | null = null;

    function open(focus = true) {
      lastFocus = document.activeElement as HTMLElement;
      info!.hidden = false;
      info!.setAttribute('aria-hidden', 'false');
      document.body.classList.add('overlay-open');
      requestAnimationFrame(() => info!.classList.add('is-open'));
      if (focus) info!.querySelector<HTMLElement>('[data-info-close]')?.focus();
    }
    function close() {
      info!.classList.remove('is-open');
      document.body.classList.remove('overlay-open');
      window.setTimeout(() => {
        info!.hidden = true;
        info!.setAttribute('aria-hidden', 'true');
      }, 300);
      lastFocus?.focus();
    }
    function openToForm() {
      open(false);
      requestAnimationFrame(() =>
        info!.querySelector('#brief')?.scrollIntoView({ behavior: 'smooth', block: 'start' })
      );
    }

    document.querySelector('[data-info-open]')?.addEventListener('click', () => open());
    // X and the "Gallery" card both just close the menu (revealing the gallery)
    info.querySelectorAll('[data-info-close]').forEach((el) =>
      el.addEventListener('click', () => close())
    );
    info.addEventListener('click', (e) => { if (e.target === info) close(); });
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && !info.hidden) close();
    });

    // CTA on the gallery + the empty contact tee open the menu at the form
    document.querySelector('[data-open-contact]')?.addEventListener('click', openToForm);
    document.addEventListener('ppc:open-contact', openToForm);
    document.addEventListener('ppc:open-info', () => open());

    // The menu IS the homepage: open it on first load (unless deep-linking a project)
    if (!location.hash) open(false);
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initInfo);
  } else {
    initInfo();
  }
</script>
